//! JSON Schema utilities.
//!
//! Reserved for schema validation and generation helpers.
//! Currently unused — tool parameter schemas are defined inline
//! via `serde_json::json!()` in each tool's `parameters_schema()` method.
